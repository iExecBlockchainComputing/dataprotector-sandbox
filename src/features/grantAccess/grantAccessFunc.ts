import { PrivateData } from "../../App";


const grantAccessFunc = async (cNFTAddress: string, numberOfAccess: number, appRestrictAddress: string, requesterRestrictAddress: string) => {
    // const { accessHash } = await PrivateData.grantAccess(cNFTAddress, numberOfAccess, appRestrictAddress, requesterRestrictAddress)
    // return accessHash
}

export default grantAccessFunc;